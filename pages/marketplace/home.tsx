import React, { useEffect } from "react";

function Home() {
  useEffect(() => {
    const load = async () => {
      // loadMarketplace();
    };
    load();
  });

  return <div>Home</div>;
}

export default Home;
